{
  "name": "react-mono",
  "description": "",
  "scripts": {
    "play": "pnpm --filter playground run dev",
    "build": "pnpm --filter=!./playground -r run build",
    "change": "pnpm build && pnpm changeset && pnpm changeset version",
    "publish": "pnpm publish -r"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
