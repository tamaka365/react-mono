{
  "name": "react-mono",
  "description": "",
  "scripts": {
    "ui:dev": "pnpm --filter bud-ui run dev",
    "build-all": "pnpm -r run build",
    "changhe": "pnpm changeset",
    "version": "pnpm build-all && pnpm changhe && pnpm changeset version",
    "publish": "pnpm version && pnpm publish -r"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
