{
  "name": "react-mono",
  "description": "",
  "scripts": {
    "play": "pnpm --filter playground run dev",
    "build": "pnpm -r run build",
    "version": "pnpm build && pnpm changeset && pnpm changeset version",
    "publish": "pnpm publish -r"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
