{
  "name": "react-mono",
  "description": "",
  "scripts": {
    "ui:dev": "pnpm --filter bud-ui run dev",
    "build": "pnpm -r run build",
    "version": "pnpm build && pnpm changeset && pnpm changeset version",
    "publish": "pnpm version && pnpm publish -r"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
